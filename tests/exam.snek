
;; (let (a2 (fn (f) (f (f 99))))
;;    10)

;; (let (a2 (fn (f x) (f (f x))))
;;   10)

(let* ((a0 (+ 1 3))
       (a1 (fn (x y) (<= x y)))
       (a2 (fn (f x) (f (f (f x)))))
       (a3 (fn (f g) (fn (x) (f (g x)))))
      )
   0)


;; (let (a2 (fn (f x) (f (f (f x)))))
;;   10)